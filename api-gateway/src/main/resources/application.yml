server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: workout-service
              uri: http://workout-service:8081
              predicates:
                - Path=/workouts/**
